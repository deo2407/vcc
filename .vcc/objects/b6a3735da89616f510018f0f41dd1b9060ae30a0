blob 192
#ifndef OBJECT_H
#define OBJECT_H

#include <sys/types.h>

typedef enum {
    BLOB,
    TREE,
} obj_type_t;

char *serialize_obj(char *contents, size_t content_size, obj_type_t type);

#endif

#endif

